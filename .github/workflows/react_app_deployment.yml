name: Deploy React App

on: 
 push:
   branches:
     - "main"
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
	uses: actions/checkout@v2
      - name: NPM install
	run: npm ci
	working-directory: ./first-react-app
      - name: Build app
	run: npm run build
	working-directory: ./first-react-app
      - name: deploy
	uses: talhahashmi/github-pages-deploy-action@4.1.5
	with:
	  branch: react-first-app
	  folder: react-first-app/build
				
			  
